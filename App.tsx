import React from 'react'
import { SafeAreaView, View } from 'react-native'
// @ts-expect-error
import { Server_URL, Application_ID } from 'react-native-dotenv'

const App = () => {
  return <SafeAreaView />
}

export default App
